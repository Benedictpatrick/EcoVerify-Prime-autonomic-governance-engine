# ─────────────────────────────────────────────────────────
# EcoVerify-Prime Environment Configuration
# ─────────────────────────────────────────────────────────

# LLM Provider (OpenAI)
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o

# Server Ports
API_PORT=8000
MCP_SERVER_PORT=8001

# Database (SQLite checkpoint persistence)
DB_PATH=./ecoverify.db

# NHI (Non-Human Identity) Key Storage
KEYS_DIR=./keys

# OpenTelemetry (optional — omit to use console exporter only)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
